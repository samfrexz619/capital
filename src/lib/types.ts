export interface NavItems {
  path: string;
  name: string;
  id: number;
}

export interface DropdownContent {
  subject: string;
  badge: string;
  id: string;
}

export interface CtaItems {
  icon: string;
  id: string;
}